{
  "name": "task-tracker",
  "private": true,
  "scripts": {
    "dev": "pnpm --filter @task-tracker/shared build && pnpm -r --parallel --filter \"./apps/**\" dev",
    "build": "pnpm --filter @task-tracker/shared build && pnpm -r --filter \"./apps/**\" build",
    "lint": "pnpm -r --filter \"./apps/**\" lint",
    "typecheck": "pnpm --filter @task-tracker/shared build && pnpm -r typecheck",
    "format": "prettier --write ."
  },
  "devDependencies": {
    "prettier": "^3.6.2",
    "typescript": "^5.9.2"
  },
  "packageManager": "pnpm@10.34.5",
  "engines": {
    "node": ">=20"
  }
}
