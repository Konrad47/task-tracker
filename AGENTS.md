# Agent instructions

This is a pnpm monorepo: Next.js web (`apps/web`), NestJS API (`apps/api`), shared Zod contracts (`packages/shared`), MongoDB via Docker Compose.

## Maister project knowledge

Before substantial changes, read `.maister/docs/INDEX.md`. Consult the relevant documents under `.maister/docs/standards/` before editing affected code, and keep the project documentation synchronized when architecture or technology choices change.
